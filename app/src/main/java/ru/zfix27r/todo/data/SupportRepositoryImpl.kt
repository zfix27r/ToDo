package ru.zfix27r.todo.data

/*
class SupportRepositoryImpl: SupportRepository {

    override fun addNotes(addNotesReqModel: AddNotesReqModel): Flow<ResponseModel> {
        val notesDB = addNotesReqModel.notes.map {
            NoteDbEntity(
                id = 0,
                title = it.title,
                description = it.description,
                created_at = it.created_at,
                updated_at = it.updated_at
            )
        }

        return flow {
*/
/*            when (dao.addNotes(notesDB)) {
                0L -> emit(ResponseModel(UNKNOWN_ERROR))
                else ->  emit(ResponseModel(SUCCESS))
            }*//*

        }
    }
}*/
